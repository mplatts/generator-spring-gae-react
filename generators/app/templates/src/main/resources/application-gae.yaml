logging:
    pattern:
        console: '%-40.40logger{39} %n%-5p %m%n'

app:
    host: https://<%= _.unescape('${app.id}')%>.appspot.com

spring:
    contrib:
        gae:
            security:
                remember-me:
                    key: '<%= nonProdKey %>'

